services:
  test:
    build:
      context: .
      dockerfile: Dockerfile.test
    image: ha-windhager-unified-test:local
    volumes:
      - .:/app
    working_dir: /app
    environment:
      PYTHONPATH: /app
