name: CI

on:
  push:
    branches: [main]
  pull_request:

# Opt into Node 24 for JS actions until it is the runner default (Node 20 deprecation).
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: python:3.11-slim

    steps:
      - uses: actions/checkout@v6

      - name: Install dependencies
        run: pip install --no-cache-dir -r requirements_test.txt

      - name: Lint (ruff)
        run: ruff check custom_components/windhager_unified tests

      - name: Format check (black)
        run: black --check --line-length 100 custom_components/windhager_unified tests

      - name: Run tests
        run: pytest --tb=short -v

  hacs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: hacs/action@main
        with:
          category: integration
